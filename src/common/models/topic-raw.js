module.exports = function(RawTopic) {

  //RawTopic.observe('after delete', function(ctx, next) {
  //
  //  RawTopic.find(ctx.where, (err, data) => {
  //    // TODO: clean up comment, entity etc.?
  //    console.log('Deleted %s matching %j, %j, %j, %j',
  //      ctx.Model.pluralModelName,
  //      ctx.where
  //    );
  //  })
  //  next();
  //});
}
