// Initial state is common for all users. It's the ideal place for constants
// like connection string etc.
// Make sure you do setup app reducers for any new root key you will add, or else new values won't be available in redux app

// import config from './config';

export default {
  // app: {
  //   firebaseUrl: config.firebaseUrl,
  // }
};
